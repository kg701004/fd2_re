# 09 — 劇情 / 對話結構與抽取

> 怎麼把《炎龍騎士團2》的劇本從 `FDTXT.DAT` 還原成可讀、可翻譯的文字。
> ⚠ **完整劇本是遊戲著作權內容,只保留在本機 `extracted/story/`,不入版控**;
> 本文件只記錄「格式 / 方法 / 極短樣例」。

## 文本在哪、怎麼編碼

- `FDTXT.DAT`(LLLLLL 容器)共 35 個資源 ≈ **章節**;合計約 1016 條字串、58000 字。
- 文字**不是 Big5**,而是**自製字型的 uint16 字模索引**(見 `08-text-and-font-format.md`):
  glyph 0–0x71F 為 1824 個字模(0–9/A–Z 為英數,其餘為漢字),字串以 `0xFFFF` 結束。

## 對話段落結構(第 3 輪解出)

每個資源內的字串可含**多段對白**,每段格式:

```
[對話控制碼 0xFFxx] [身分 operand / 場景索引] [0x22D『] [對白 glyph…] [』]
```

- **對話控制碼**(`0xFFEE` / `0xFFED` / `0xFFEF` …):段落 / 換行 / 對話框類型。`0xFFFE` 是已驗證的軟換行，不再標成「疑為」。
- **第二個 operand 不是一律直接 DATO ID**：`0x15F84` 的 `-17/-18` 先經 `0x12C60` 嘗試以場景表／persistent record 的 `+8` 找身分，再由 record `+7` 取得 DATO；未命中的 direct-DATO fallback 與 `-19/-20` 的 runtime-unit index 路徑必須分開保存。不能把數字直接當作跨章固定角色／肖像表索引。
- `0x22D` ≈ 開引號 `『`。

### 控制碼語意(已反組譯文本渲染器 `0x15F84`，外層嘴型／輸入在 `0x16D00+`)

控制碼以 sign-extend int8 比較分派(`cmp eax, imm8`)。各碼語意:

| 碼 | int8 | 語意 |
|---|---|---|
| `0xFFEF` | -17 | **開上框 + 載入 DATO**；operand 先走 `0x12C60` 身分查找，未命中時才可作 direct-DATO fallback |
| `0xFFEE` | -18 | **開下框 + 載入 DATO**；operand／record provenance 由 `0x12C60` 路徑決定，不命名成固定肖像 ID |
| `0xFFED` | -19 | **開上框 + runtime unit lookup**；operand 是 unit index，最後讀該 record `+7` 作 DATO selector |
| `0xFFEC` | -20 | **開下框 + runtime unit lookup**；operand 是 unit index，最後讀該 record `+7` 作 DATO selector |
| `0xFFFE` | -2 | **句內換行**;每框最多 3 行,滿則 `0x17C24` 捲動 / 等待 |
| `0xFFFD` | -3 | **換行 + 等待按鍵翻頁**(`0x17A57(1)` 清框續下一頁) |
| `0xFFFC` | -4 | 遞迴插入 `[0x53AD9]` 字串 |
| `0xFFFA`/`0xFFFB` | -6/-5 | 遞迴插入 `[0x53AE1]`／`[0x53ADD]` 名稱或數值字串，不是特效 |
| `0xFFFF` | -1 | **字串結束** |

對話框位置全域 `[0x3C67]`:`0x728`=上框、`0x9017`=下框。文字起點 `0xA0B4F`(上)/`0xA951F`(下)。
**重要副產物**:四種開框碼最終都會在其各自 provenance 路徑讀取 `DATO.DAT`；`DATO.DAT` 是人物頭像／立繪資源，但 operand 數字本身不一定就是 DATO archive index。

> 對重製:框開碼=換說話者(本工具已據此分行並標說話者名);`0xFFFE`=軟換行;`0xFFFD`=「按鍵翻頁」點。
> 翻譯改寫文字長度時,要保留框開碼與其 raw identity operand／查找 provenance,並注意每框 3 行的排版上限。

## 抽取方法

1. `tools/decode_text.py` 解析字串為 glyph 索引序列。
2. `tools/render_story.py` 用自製字型把整章渲染成**可讀 PNG**(分頁,控制碼當換行)。
3. 對照 PNG 人工 / 多模態轉錄為文字,說話者依肖像 ID 標註 → 存 `extracted/story/`(本機)。

```bash
# 把全部 35 章渲染成可讀 PNG(需自備原版,先 unpack_dat + extract_all)
python3 tools/render_story.py --all extracted/raw/FDTXT \
        extracted/raw/FDOTHER/FDOTHER_004.bin extracted/story
```

## 極短樣例(序章開場,示意格式)

> 完整劇本見本機 `extracted/story/`,此處僅示意說話者標註格式:

```
索爾  :累死了,大家休息一下吧!
亞雷斯:聽說再越過這片海洋就到馬拉大陸了…
悠妮  :嗯,還好。海風吹起來真舒服啊…
```

## 對中文化 / 重製的意義

- 已能把劇本還原成可讀文字 + 說話者 → 可做劇情校訂、翻譯、語音對照。
- 下一步:建 **glyph 索引 → Unicode 對照表**(讓劇本可全文檢索 / 程式化重打),
  並還原控制碼語意(才能安全改寫文字長度與換行)。

## 進度

- ✅ 文本 / 字型格式破解;✅ 對話段落結構(說話者 + 控制碼)解出;✅ 全 35 章渲染成可讀 PNG。
- ✅ **glyph→Unicode 對照表完成(1824/1824,100% 覆蓋)** → `docs/data/glyph_map.json`。
- ✅ **全 35 章劇情轉錄完成**:`tools/decode_story_text.py --all` 自動解碼成含說話者名的 UTF-8,
  輸出本機 `extracted/story/full_story_auto.md`(1450 句對白)。序章+第2+第3章另有人工精校版。
- ⬜ 控制碼語意精確化(換行 / 名稱插入 / 顏色);少數罕用字模可能仍有個別誤判,可隨閱讀校正。

## 劇情大綱(自動解碼後可讀)

開場(序章):索爾、亞雷斯、悠妮渡海抵馬拉大陸遇海盜 → 哈瓦特父子(哈諾)相助入隊。
第2章羅德鎮強盜團、希莉亞登場、揭露悠妮失憶疑為公主。第3章救鐵諾入隊、反派葛雷與卡蘿娜伏線。
中段轉職、世界各地;後段**天空之城機器人篇**(機甲守衛以型號代號 73/C2/A1 等對話,悠妮一度被機械控制)。
結局眾人保住世界、返鄉,希莉亞請父王(王國)辦慶功宴(印證其公主身份)。
完整逐章見本機 `extracted/story/full_story_auto.md`。

## remake 對話框渲染規則(cmd/fd2/main.go;使用者實玩逐項校正 2026-07-05)

原版對照:我方(角色 id 0–31)= **下框、頭像在左**;對方/NPC(id≥32,如父王48/王后66)= **上框、頭像在右**;
頭像側臉朝文字。以下為對原版截圖逐項修正的規則(commit 57c0e30 / dc5ebb1 / b81268d):

- **文字寬 vs 頭像**:文字換行右緣須**止於頭像側之前**,不得覆蓋頭像。下框到框右緣;上框(頭像在右)
  到頭像左緣前 8px(`rightEdge = hx-8`)。原版父王對話文字佔左~60%、碰頭像前換行(orig 18-02-20)。
- **框位置(框高 198,logicalH=400)**:下框 `by=198`(底邊 396 在畫面內);上框 `by=4`(頂邊在畫面內)。
  舊值(下 224/上 -22)使邊線出畫面看不到。頭像垂直置中框內 `hy=by+(198-168)/2`,不凸出框上下邊。
- **框內底色 = 頭像底色漸層**:頭像背景是漸層藍(頂 40,69,138 → 底 56,85,154);框素材 `assets/ui/dialog.png`
  內部均勻 56,85,154(僅等於頭像底部亮色)。故框內疊同一漸層(`g.dlgGrad`),消除頭像↔框接縫色差。
- **[HARD] 長對白分頁,不截斷**:一句 >3 顯示行要**分頁**(每頁 3 行),Enter 有下一頁先翻頁、翻完才換句。
  王座對白多句超 3 行(line6/12/14/15/17…,如 line17 57字=5行=2頁)。抽 `dlgWrap`(繪製與分頁共用換行)
  + `dlgPageCount` + `dlgAdvance`(翻頁/pop),`g.dlgPage` 記頁碼、新對白歸零。**舊碼繪製迴圈 `i<3` 只畫 3 行
  其餘丟棄 → 後半永遠看不到**(使用者實玩抓到)。回歸測試 `cmd/fd2/dlg_test.go`(需 xvfb 跑,ebiten init 要顯示器)。
- **面向**:cutscene 角色 dir 預設 0(下,面向玩家);走位者走完面向 actor 目標 dir(進場走位=`a.Dir`,
  如亞雷斯走到索爾旁面向他);詳見 doc47 §11 面向通用規則。
> ⛔ **[remake 證據排除]**(2026-09-04):以下「驗證法」整套建立在 remake 的 headless
> debug hook 上,依現行判準**不構成原版事實的證據**。凡是靠這條路徑得出的劇情/對白
> /走位結論一律視為未驗證,需改以 DOSBox-X 原版實機重做。此段保留供追溯,不得引用
> 為既定事實。見 `docs/data/remake_excluded_claims.json`。

- **驗證法**:`FD2_CAMPAIGN=… FD2_CAMP_NODE=<node> FD2_SHOT=out.png FD2_SHOT_FRAME=N`(headless,host xvfb+llvmpipe);
  長對白翻頁用 xdotool 送 Enter。⚠ 軟體渲染 fps≠60,`FD2_SHOT_FRAME`(遊戲幀)與 wall-clock 送鍵易對不上,
  優先 Go 測試(確定性)驗邏輯、截圖驗版面(見記憶 [[fd2-goal-and-no-speculation-rule]] 採樣率不準)。

---

## 2026-09-04:原版側驗證法(取代被排除的 remake 截圖法),以及一個被它抓出來的缺陷

本文件開頭那套「驗證法」是 remake headless 截圖,已依 2026-09-04 判準排除
(見 `docs/data/remake_excluded_claims.json`)。本節建立**原版側的替代方法**,
並用它做了第一次真實比對。

### 方法

1. DOSBox-X 跑原版,用**固定等待**推進對白——**不要用 `wait-settle`**
   (它會停在轉場開始前的舊畫面,見 `48-dosbox-x-debugger-build.md` §9.2)。
2. 截圖,讀畫面上的對白文字與立繪。
3. 與 `tools/decode_story_text.py` 對 `extracted/raw/FDTXT/FDTXT_NNN.bin` 的解碼結果比對。

### 第一次比對:ch01 王宮場景 vs `FDTXT_033`

原版實機截到的三句,與解碼文本**逐字吻合**:

| 原版畫面上的文字 | `FDTXT_033` 解碼 |
|---|---|
| 你來啦!起來起來,這裡沒有別人,不用多禮。 | 第 2 句,一字不差 |
| 這一陣子國務繁忙,我們父子兩個也很久沒聚聚了。 | 第 4 句,一字不差 |
| 兒臣並未繼承羅特帝亞王家的血脈,照理來說,兒臣以為應該由皇弟迪恩來繼承王位‥‥ | 第 12 句,一字不差 |

**文字管線(容器→解碼→字模)在原版側得到 3/3 驗證。**

### ⚠ 同一次比對抓到的缺陷:**說話者標記不可信**

三句裡有兩句的說話者標記是錯的:

- 第 2、4 句在原版畫面上的立繪是**國王**(淺髮、留鬍、白金衣),解碼卻標成 **索爾**。
- 第 12 句的立繪是**索爾**(藍髮、紅頭巾),標記也是索爾——**這一句對**。

第三句正好確立了「**立繪 = 說話者**」這個慣例(它的文字內容是兒臣對父王的自稱,
與索爾的立繪一致),因此前兩句是真的標錯,不是慣例理解有誤。

**第二個獨立佐證,而且來自本文件自己**:上方「remake 對話框渲染規則」一節記載
(該規則是依原版截圖逐項校正出來的,屬原版側觀察):
**我方(角色 id 0–31)= 下框、頭像在左;對方/NPC(id≥32,如父王 48、王后 66)= 上框、
頭像在右**。本輪三張原版截圖完全符合這條規則:

| 句 | 原版畫面 | 依本文件規則的說話者 | 解碼標記 |
|---|---|---|---|
| 第 2 句「你來啦!…」 | **上框、頭像在右** | NPC(父王) | 索爾 ❌ |
| 第 4 句「…國務繁忙…」 | **上框、頭像在右** | NPC(父王) | 索爾 ❌ |
| 第 12 句「兒臣並未繼承…」 | **下框、頭像在左** | 我方(索爾) | 索爾 ✅ |

也就是說,判定不必仰賴我對立繪長相的辨識——**框的位置與頭像側邊就足以定說話者陣營**,
而它與解碼標記在前兩句上直接衝突。

第三個佐證:`FDTXT_033` 全檔的說話者只有 索爾/亞雷斯/哈瓦特/哈諾 四個,
**名單裡根本沒有國王**,而王宮場景顯然有國王在說話。

**因此:`decode_story_text.py` 的文字內容可信(原版 3/3),但它的說話者欄位
在至少 ch01 王宮這一段是錯的,不可當作已驗證資料使用。** 根因(名字索引解析,
或這一段用了不在名表內的角色)未查,不猜。

> 這正是排除 remake 驗證法的價值所在:remake 是**照著同一份解碼結果**畫的,
> 用它截圖比對只會自我印證,不可能發現說話者標錯——要靠原版的立繪才看得出來。

### 2026-09-04 續:說話者標錯的**根因已查明**

前一段把 `decode_story_text.py` 的說話者欄位標為不可信,但註明「根因未查,不猜」。本段查明。

#### 程式碼

```python
spk  = seg[0]                       # 開框控制碼後的 operand
name = PORT.get(spk, g2s([spk]))    # 直接查硬編碼表
PORT = {0: "索爾", 1: "哈諾", 2: "鐵諾", 3: "哈瓦特", 4: "亞雷斯", 5: "洛娜", ...}
```

**它完全不看是哪一個開框碼**,`0xFFEC`~`0xFFEF` 一律用同一張表解同一個 operand。

#### 為什麼這是錯的(本文件自己早就寫過)

上方「控制碼語意」節記載:

| 碼 | 語意 |
|---|---|
| `0xFFEF`/`0xFFEE` | 開上/下框 + **載入 DATO**;operand 先走 `0x12C60` 身分查找 |
| `0xFFED`/`0xFFEC` | 開上/下框 + **runtime unit lookup**;operand 是 **unit index**,最後讀該 record `+7` 作 DATO selector |

並明寫「**不能把數字直接當作跨章固定角色／肖像表索引**」。
`decode_story_text.py` 做的正是被禁止的那件事。

#### 實證:ch01 王宮場景的實際 operand

| 框 | 開框碼 | operand | 解碼器標記 | 原版畫面 |
|---|---|---|---|---|
| 1 | `0xFFEE`(下框) | 0 | 索爾 | —— |
| **2** | **`0xFFED`(上框,runtime unit)** | **0** | **索爾** | **上框右頭像 = NPC(國王)** ❌ |
| 3 | `0xFFEE`(下框) | 0 | 索爾 | —— |
| 4 | `0xFFED`(上框) | 0 | 索爾 | ❌ |

第 2 框正是「你來啦!起來起來…」那句。**開框碼是 `0xFFED`(上框)**,而本文件的框位規則是
「下框+頭像左 = 我方;上框+頭像右 = NPC」,原版截圖亦為上框右頭像。
所以 operand `0` 在 `0xFFED` 之下**不是索爾**,而是該場景執行期 unit 0 —— 國王。

#### 修正方向(**2026-09-04 已實作**,見下節)

說話者解析必須**依開框碼分流**:

* `0xFFEF`/`0xFFEE`:operand 走 `0x12C60` 的身分查找,未命中才回退 direct-DATO;
* `0xFFED`/`0xFFEC`:operand 是 runtime unit index,**靜態解碼無法解出姓名**——
  該欄位應輸出「unit#N(執行期決定)」而不是硬套 `PORT`。

⚠ 後者意味著**部分說話者在純靜態解碼下本來就不可知**,因為它取決於執行期名冊。
硬給一個名字必然是猜的——這正是目前的錯誤形態。

**文字內容不受影響**(已於前一段用原版 3/3 逐字驗證),只有說話者欄位需要修。

### 2026-09-04(續二):分流已實作,並量出全域影響

`decode_story_text.py` 新增 `resolve_speaker(leading, operand)`,依開框碼分流:

| 開框碼 | operand 語意 | 現在輸出 |
|---|---|---|
| `0xFFEE`/`0xFFEF` | 身分索引(`0x12C60` 查找) | 角色名(同舊行為) |
| `0xFFED`/`0xFFEC` | **執行期 unit index** | `unit#N(執行期決定)` |
| 其他 | —— | `?#N(開框碼 …)` |

**全域影響(先量再改)**:掃過全部 35 個 FDTXT、1450 個對話框——

| 開框碼 | 框數 | 靜態可解? |
|---|---|---|
| `0xFFEE` 下框+身分查找 | 813 | ✅ |
| `0xFFEF` 上框+身分查找 | 364 | ✅ |
| `0xFFED` 上框+runtime unit | 231 | ❌ |
| `0xFFEC` 下框+runtime unit | 42 | ❌ |

**273/1450 = 18.8% 的說話者過去是被硬套上去的名字。** 這把前一節「ch01 王宮兩句」
的個案擴大成全域數字。實測也確認**沒有任何一個框沒有前導開框碼**,
所以 `leading is None` 的分支只是防禦性的,不是真實情形。

#### 驗證方式(不是只看程式碼改對了)

判別性案例用 doc09 自己以原版截圖判定的那三句,**兩個方向都測**:

```
  1 索爾                   兒臣索爾，晉見父王陛下。
  2 unit#0(執行期決定)      你來啦！起來起來，這裡沒有別人，不用多禮。   ← 原本標「索爾」
  4 unit#0(執行期決定)      到這裡來，讓父王好好看看你…                ← 原本標「索爾」
  5 unit#1(執行期決定)      是啊！我們一家人，難得這樣在一起。
 12 索爾                   兒臣不敢。但…兒臣並未繼承羅特帝亞王家的血脈  ← 對照組,必須維持
```

**獨立佐證(不靠我改的那段程式碼)**:被改標的那些框,內容自稱「朕」、說
「讓父王好好看看你」「朕希望能由你來接任羅特帝亞的王位」——是國王;`unit#1`
說「我們一家人」——是王后。文字內容本身早已用原版逐字驗證過 3/3,
所以它是獨立於本次修改的訊號。

新增 `tools/test_decode_story_text.py`(8 項)。刻意同時鎖兩個方向:只測
「runtime 框不給名字」的話,把函式寫成「一律回傳 unit#N」也會通過。
**故障注入**:拿掉 runtime 分支 → 3 項失敗;還原 → 全數通過。全套 59 個測試綠燈。

⚠ 仍未解決的部分:那 273 個框的**真實說話者**依然未知,而且**靜態解不出來**——
它取決於執行期名冊。要知道是誰,只能在原版活體讀該 unit record 的 `+7`。
本次改的是「不再給錯的名字」,不是「給出對的名字」。
