# remake/internal/battle/model.go 對這個檔案的內容做 byte-exact SHA256 驗證
# (提取自 FDFIELD.DAT 的 native turn-event 資料表,見 nativeTurnEventCatalogSHA256)。
# Windows checkout 若套用 core.autocrlf 把 LF 轉成 CRLF,會讓雜湊比對失敗,導致
# HasNativeTurnEventControlState 恆為 false、event62/63 整條鏈路失效。強制此檔
# 永遠以 checked-in 的原始位元組(LF)存在,不受使用者 autocrlf 設定影響。
remake/assets/maps/native_turn_event_controls.json -text

# tools/*.sh 都是給 WSL2 bash 執行的(dosbox-x harness、ffmpeg 擷取等)。同樣的
# core.autocrlf 問題:Windows checkout 會把 LF 轉成 CRLF,WSL bash 遇到 CRLF 腳本
# 會直接 syntax error(shebang 行、heredoc 都會壞)。強制這個目錄下的 .sh 一律用
# LF checkout,不受使用者 autocrlf 設定影響。
#
# 2026-09-03:pattern 從 `tools/*.sh` 放寬成 `*.sh`。原本的 pattern 不遞迴,
# `tools/docker/*.sh` 兩支完全沒被涵蓋,實測在 WSL bash 下 syntax error。
# 另外 `tools/*.sh` 底下 4 支(export_fm/export_mt32/export_music_ogg/
# extract_fd2_video_frame)雖然有這條規則,但檔案是在規則加入(2026-08-24)之前
# checkout 的,index 是 LF、working tree 仍是 CRLF,規則從未對它們生效——已用
# `git add --renormalize` + 重新 checkout 修正。
*.sh text eol=lf
